#include <stdio.h>

// Exercício Beecrowd 1004 

int main() {
 
    int A, B, PROD;
    // Leitura das variáveis
    scanf("%d %d", &A, &B);
    
    //Multiplicação das variáveis
    PROD = A * B;
    
    //Saída
    printf("PROD = %d\n", PROD);
 
    return 0;
}
