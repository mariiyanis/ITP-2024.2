#include <stdio.h>
 // Exercício Beecrowd 1003

int main() {
 
   int A, B, soma;
   
   //Leitura das variáveis
   scanf("%d %d", &A, &B);
   
   //Somatória
   soma = A + B;
   
   //Saída
   printf("SOMA = %d\n", soma);
 
    return 0;
}
